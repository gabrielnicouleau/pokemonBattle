{% extends 'base.html.twig' %}

{% block title %}Bienvenu sur PokemonBattle!{% endblock %}

{% block body %}
    <body class="bg-gray-500">
        <h3 class="text-white text-center uppercase text-xl my-3">Liste des pokémons</h3>
        {% for pokemon in pokemons %}
            {# <img src="{{ pokemon.image }}" alt="pokemon">
            <p>n°{{ pokemon.id }} nom: {{ pokemon.nom }}</p>
            <p>Stats</p>
            <p>PV: {{ pokemon.pointDeVie }}</p>
            <p>Attaque: {{ pokemon.pointAttaque }}</p>
            <p>Defense: {{ pokemon.pointDefense }}</p> #}

            <!-- Card -->
            <div class="mx-96 my-3">
                <div class="group flex flex-col bg-white border border-gray-200 shadow-sm rounded-xl dark:bg-neutral-900 dark:border-neutral-700 dark:shadow-neutral-700/70">
                    <div class="flex flex-col justify-start items-center bg-blue-600 rounded-t-xl">
                    <img width="150" height="150" viewBox="0 0 56 56" src="{{ pokemon.image }}">
                    </div>
                    <div class="p-4 md:p-6 text-center">
                    <span class="block mb-1 text-xl font-semibold uppercase text-blue-600 dark:text-blue-500">
                        n°{{ pokemon.id }}: {{ pokemon.nom }}
                    </span>
                    <h3 class="text-xs font-semibold text-gray-800 dark:text-neutral-300 dark:hover:text-white">
                        Stats
                    </h3>
                    <p class="mt-3 text-gray-500 dark:text-neutral-500">
                        PV: {{ pokemon.pointDeVie }}
                    </p>
                    <p class="mt-3 text-gray-500 dark:text-neutral-500">
                        Attaque: {{ pokemon.pointAttaque }}
                    </p>
                    <p class="mt-3 text-gray-500 dark:text-neutral-500">
                        Defense: {{ pokemon.pointDefense }}
                    </p>
                    </div>
                </div>
            </div>
        <!-- End Card -->
        {% endfor %}
    </body>
{% endblock %}
